kind: ConfigMap
apiVersion: v1
metadata:
  name: zerotrace-agent
  namespace: yunshan-zerotrace
data:
  zerotrace-agent.yaml: |-
    controller-ips:
    - 10.1.4.1
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: zerotrace-agent
  namespace: yunshan-zerotrace
spec:
  selector:
    matchLabels:
      app: zerotrace-agent
  template:
    metadata:
      annotations:
        container.apparmor.security.beta.kubernetes.io/zerotrace-agent: unconfined
      name: zerotrace-agent
      namespace: yunshan-zerotrace
      labels:
        app: zerotrace-agent
    spec:
      hostNetwork: true
      hostPID: true
      containers:
        - name: zerotrace-agent
          image: zerotrace-agent:latest
          imagePullPolicy: Always
          securityContext:
            privileged: true
          volumeMounts:
            - name: sys-kernel-debug
              mountPath: /sys/kernel/debug
            - name: zerotrace-agent-conf
              mountPath: /etc/zerotrace-agent
            - name: logs
              mountPath: /var/log/zerotrace-agent/
              subPath: ""
      volumes:
        - name: sys-kernel-debug
          hostPath:
            path: /sys/kernel/debug
        - name: zerotrace-agent-conf
          configMap:
            name: zerotrace-agent
        - name: logs
          hostPath:
            type: DirectoryOrCreate
            path: /var/log/zerotrace-agent
---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: zerotrace-agent-role
rules:
- apiGroups: [""]
  resources:
  - nodes
  - namespaces
  - configmaps
  - services
  - pods
  - replicationcontrollers
  verbs: ["get", "list", "watch"]
- apiGroups: ["apps"]
  resources:
  - daemonsets
  - deployments
  - replicasets
  - statefulsets
  verbs: ["get", "list", "watch"]
- apiGroups: ["extensions", "networking.k8s.io"]
  resources: ["ingresses"]
  verbs: ["get", "list", "watch"]
- apiGroups: ["route.openshift.io"]
  resources: ["routes"]
  verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: zerotrace-agent-view-binding
subjects:
  - kind: ServiceAccount
    name: default
    namespace: yunshan-zerotrace
roleRef:
  kind: ClusterRole
  name: zerotrace-agent-role
  apiGroup: rbac.authorization.k8s.io
