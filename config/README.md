# config/

This directory contains **YAML configuration examples only**.  
Rust configuration parsing code lives at `src/config/`.

Files:
- `zerotrace-agent.yaml` — default agent configuration
- `zerotrace-agent-standalone.yaml` — standalone mode  
- `zerotrace-agent-windows.yaml` — Windows platform adjustments
