[package]
name = "zerotrace-kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
zerotrace-core = { path = "../zerotrace-core" }
async-trait = "0.1"
parking_lot = "0.12"
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["sync", "rt", "macros"] }
