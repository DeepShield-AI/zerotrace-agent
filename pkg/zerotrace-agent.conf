# ZeroTrace agent
#
# ZeroTrace agent daemon.

description     "Zerotrace Agent"

start on runlevel [2345]
stop on runlevel [!2345]

env RUST_BACKTRACE=1
env LD_LIBRARY_PATH=/usr/local/zerotrace/deps/

respawn
respawn limit 10 5
umask 022

exec /usr/sbin/zerotrace-agent

